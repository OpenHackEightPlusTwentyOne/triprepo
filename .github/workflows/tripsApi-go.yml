name: Go

on:
  push:
    branches: [ master ]
    paths: 
    - apis/trips/** 
    - .github/workflows/tripsApi-go.yml
  pull_request:
    branches: [ master ]
    paths: 
    - apis/trips/** 
    - .github/workflows/tripsApi-go.yml

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

    - name: Set up Go 1.x
      uses: actions/setup-go@v2
      with:
        go-version: ^1.13
      id: go

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    - name: Get dependencies
      working-directory: apis/trips/
      run: |
        go get -v -t -d ./...
        if [ -f Gopkg.toml ]; then
            curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
            dep ensure
        fi

    - name: Build
      working-directory: apis/trips/
      run: go build -v .

    - name: Test
      working-directory: apis/trips/
      run: go test -v .
      
    - name: 'Login via Azure CLI'
      uses: azure/login@v1
      with:
        creds: ${{ secrets.JB_AZURE_CREDENTIALS }}
    - name: 'Build and push image'
      uses: azure/docker-login@v1
      with:
          login-server: ${{ secrets.JB_REGISTRY_LOGIN_SERVER }}
          username: ${{ secrets.JB_REGISTRY_USERNAME }}
          password: ${{ secrets.JB_REGISTRY_PASSWORD }}
          working-directory: apis/trips/
    - run: |
          docker build . -t openhackrj195ck7acr.azurecr.io/tripapigo:${{ github.sha }}
          docker push openhackrj195ck7acr.azurecr.io/tripapigo:${{ github.sha }}
       
            
